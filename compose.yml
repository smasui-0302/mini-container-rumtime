version: "3.8"
services:
  dev:
    image: golang:1.24
    privileged: true
    cap_add:
      - NET_ADMIN
      - SYS_ADMIN
      - SYS_PTRACE
    volumes:
      - .:/work
    working_dir: /work
    tty: true
